[project]
name = "icanpython"
version = "0.1.0"
description = "hallo python world!"
authors = [
    {name = "elzabe"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests (>=2.33.1,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false